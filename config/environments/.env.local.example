# ==============================================================================
# DEVELOPMENT ENVIRONMENT VARIABLES - MPD DASHBOARD
# ==============================================================================

# Copy this file to .env.local and adjust paths as needed

# Node.js Environment
NODE_ENV=development

# ==============================================================================
# DOCUMENT STORAGE CONFIGURATION (DEVELOPMENT)
# ==============================================================================

# Primary documents path (optional in development)
# DOCUMENTS_PATH=/custom/path/to/documents

# Backend storage path - matches the concurso-backend project
# This is the recommended path for development when running both frontend and backend
BACKEND_STORAGE_PATH=B:\CODE\PROYECTOS\concursos-mpd\concurso-backend\storage\documents

# Legacy documents path (currently used with existing data)
LOCAL_DOCUMENTS_PATH=B:\concursos_situacion_post_gracia\descarga_administracion_20250814_191745\documentos

# Backend-aligned configuration (matches Spring Boot StorageConfig.java)
APP_STORAGE_BASE_DIR=./storage
APP_STORAGE_DOCUMENTS_DIR=documents

# Alternative paths for different operating systems:
# LOCAL_DOCUMENTS_PATH=C:\mpd\documents                    # Alternative Windows
# LOCAL_DOCUMENTS_PATH=/Users/username/mpd/documents      # macOS
# LOCAL_DOCUMENTS_PATH=/home/username/mpd/documents       # Linux

# ==============================================================================
# API CONFIGURATION (DEVELOPMENT)
# ==============================================================================

# Backend Spring Boot API (local development)
BACKEND_API_URL=http://localhost:8080
BACKEND_API_TIMEOUT=30000

# ==============================================================================
# DATABASE CONFIGURATION (DEVELOPMENT)
# ==============================================================================

# MySQL Connection (for direct queries in development)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=mpd_concursos
DB_USER=root
DB_PASSWORD=root1234
DB_DATABASE=mpd_concursos

# ==============================================================================
# AUTHENTICATION & SECURITY (DEVELOPMENT)
# ==============================================================================

# JWT Configuration (development only - not for production)
JWT_SECRET=dev_jwt_secret_key_for_development_only_minimum_256_bits_required
JWT_EXPIRES_IN=24h
REFRESH_TOKEN_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=10

# ==============================================================================
# APPLICATION SETTINGS (DEVELOPMENT)
# ==============================================================================

# Next.js Configuration
NEXTAUTH_URL=http://localhost:9002
NEXTAUTH_SECRET=development-nextauth-secret-key

# Logging and Monitoring
LOG_LEVEL=debug
ENABLE_DEBUG_LOGS=true

# Performance Settings
SLOW_QUERY_THRESHOLD=1000
ENABLE_PERFORMANCE_MONITORING=true

# Cache Settings  
CACHE_DURATION=30000

# ==============================================================================
# DEVELOPMENT NOTES
# ==============================================================================

# 1. Copy this file to .env.local for your local development setup
# 2. Adjust BACKEND_STORAGE_PATH to match your backend project location
# 3. Set LOCAL_DOCUMENTS_PATH to your legacy documents folder if different
# 4. The system will automatically try multiple paths in development:
#    - Backend storage path (recommended)
#    - Legacy documents path 
#    - Current directory storage folders
# 5. Monitor logs to see which path is being used: "📁 Document base paths (development)"
