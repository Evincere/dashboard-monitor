# ==============================================================================
# PRODUCTION ENVIRONMENT VARIABLES - MPD DASHBOARD
# ==============================================================================

# Node.js Environment
NODE_ENV=production

# ==============================================================================
# DOCUMENT STORAGE CONFIGURATION (ALIGNED WITH SPRING BOOT BACKEND)
# ==============================================================================

# Primary documents path (highest priority) - Override for custom deployments
DOCUMENTS_PATH=/app/storage/documents

# Backend-aligned configuration (matches Spring Boot StorageConfig.java)
# These variables mirror the app.storage.* properties from the Spring Boot backend
APP_STORAGE_BASE_DIR=/app/storage
APP_STORAGE_DOCUMENTS_DIR=documents

# Development paths
# Path to legacy documents (currently used in development)
LOCAL_DOCUMENTS_PATH=B:\concursos_situacion_post_gracia\descarga_administracion_20250814_191745\documentos

# Backend storage path for development (when running both frontend and backend locally)
BACKEND_STORAGE_PATH=B:\CODE\PROYECTOS\concursos-mpd\concurso-backend\storage\documents

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================

# Backend Spring Boot API
BACKEND_API_URL=http://localhost:8080
BACKEND_API_TIMEOUT=30000

# MySQL Connection (for direct queries)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=mpd_concursos
DB_USER=root
DB_PASSWORD=root1234

# ==============================================================================
# AUTHENTICATION & SECURITY
# ==============================================================================

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-key-here
JWT_EXPIRES_IN=24h
REFRESH_TOKEN_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=12

# ==============================================================================
# APPLICATION SETTINGS
# ==============================================================================

# Next.js Configuration
NEXTAUTH_URL=http://localhost:9002
NEXTAUTH_SECRET=your-nextauth-secret-here

# Logging and Monitoring
LOG_LEVEL=info
ENABLE_DEBUG_LOGS=false

# ==============================================================================
# DEPLOYMENT SPECIFIC SETTINGS
# ==============================================================================

# Docker/Container specific paths
# DOCUMENTS_PATH=/var/lib/mpd-documents
# DOCUMENTS_PATH=/opt/mpd/documents

# Kubernetes volume mount paths
# DOCUMENTS_PATH=/mnt/shared-storage/documents

# NFS/Network storage paths
# DOCUMENTS_PATH=/mnt/nfs/mpd-documents

# ==============================================================================
# NOTES FOR DEPLOYMENT
# ==============================================================================

# 1. Set DOCUMENTS_PATH to the correct path where documents are stored in production
# 2. Ensure the application has read permissions to the document directory
# 3. For containerized deployments, mount the document volume appropriately
# 4. The system will try multiple fallback paths if primary path fails
# 5. Monitor logs to verify correct path resolution: "📁 Document base paths (production)"
