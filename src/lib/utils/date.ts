import { es } from 'date-fns/locale';

export const spanishDateConfig = {
    locale: es,
    formatLong: {
        date: 'd MMM yyyy',
        time: 'HH:mm',
        dateTime: 'd MMM yyyy HH:mm'
    }
};
